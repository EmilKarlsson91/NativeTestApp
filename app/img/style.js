import {
  StyleSheet
} from 'react-native';

const style = StyleSheet.create({
  img: {
    height: 270,
    width: 270,
    borderWidth: 2,
    borderColor: 'orange',
    borderRadius: 131,
  },
});

export default style;
