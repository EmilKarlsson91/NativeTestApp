import {
  StyleSheet
} from 'react-native';

const style = StyleSheet.create({
  img: {
    height: 200,
    width: 200,
    borderWidth: 2,
    borderColor: 'orange',
    borderRadius: 100,
  },
});

export default style;
